<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.xm.service.dao.login.StoreDAO" >
    <select id="queryStore" resultType="com.xm.service.apiimpl.pc.login.dto.StoreDTO" parameterType="map">
        SELECT
          FACTORY as factory,
          STORE_MIN as storeMin,
          STORE_MAX as storeMax,
          STEPID as stepid
        FROM
          STORE
    </select>

    <update id="updataStore"  parameterType="map">
        UPDATE STORE SET
        STORE_MIN = #{storeMin},
        STORE_MAX = #{storeMax}
        WHERE
        FACTORY = #{factory} AND
        STEPID = #{stepid}
    </update>

    <select id="loadByPrimaryKey" parameterType="map" resultType="map">
        SELECT * from STORE
        where FACTORY = #{FACTORY} and
        STEPID = #{STEPID}
    </select>
    <insert id="addData" parameterType="map">
        INSERT INTO  STORE
        (FACTORY,   STORE_MIN,   STORE_MAX,   STEPID) VALUES
        (#{FACTORY},#{STORE_MIN},#{STORE_MAX},#{STEPID})
    </insert>
    <update id="updateData" parameterType="map">
        UPDATE STORE SET
        STORE_MIN = #{STORE_MIN} ,
        STORE_MAX = #{STORE_MAX}
        WHERE
        FACTORY = #{FACTORY} and
        STEPID = #{STEPID}
    </update>
</mapper>